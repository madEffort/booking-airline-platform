from django.urls import path

urlpatterns = [
    # path('tickets/', TicketListView.as_view(), name='list_tickets'),
    # path('purchase/<int:pk>/', TicketPurchaseView.as_view(), name='purchase_ticket'),
    # path('tickets/<int:pk>/refund/', TicketRefundView.as_view(), name='refund_ticket'),
]